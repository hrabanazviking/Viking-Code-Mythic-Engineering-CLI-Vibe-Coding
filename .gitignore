__pycache__/
*.pyc
.coverage
.mypy_cache/
.ruff_cache/
.pytest_cache/
build/
dist/
*.egg-info/

# Obsolete Phase 1 research partial — kept on disk as reference but
# never tracked. Flagged in PHASE1_RUNTIME_AUDIT.md (F-018). Remove
# this line if you want to bring the file back into version control.
research_data/vibe_research_part1_tier1a.md

# Stray absolute-path leak from a test that wrote to a literal
# "Users/volma/AppData/..." string instead of a proper temp dir.
# The directory contents are pure debris (out.txt files in deeply
# nested junk paths). Ignored so the leak doesn't pollute the
# tracked tree; the underlying test fix should land separately.
Users/
